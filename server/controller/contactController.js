const contact = require('../model/contacts')
const contactTemplate = require('../model/contacts')

module.exports = {
  getAllContacts : (req, res) => {
    
  },

  addContact : (req, res) => {
    const contact = new contactTemplate({
      
    })
  }
}